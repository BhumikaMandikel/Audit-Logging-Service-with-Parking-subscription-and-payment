# Force LF for shell scripts
*.sh text eol=lf

# Keep Python, JS, JSON, etc. with standard settings
*.py text
*.js text
*.json text
*.html text
*.css text

# Optional: enforce LF for Dockerfile or YAML
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
